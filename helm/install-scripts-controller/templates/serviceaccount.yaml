{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "install-scripts-controller.serviceAccountName" . }}
  labels:
    {{- include "install-scripts-controller.labels" . | nindent 4 }}
{{- end }}
