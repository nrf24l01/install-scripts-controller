services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      CONFIG_PATH: /app/config.yml
    volumes:
      - ./config.yml:/app/config.yml:ro
      - app-data:/data
    restart: unless-stopped

volumes:
  app-data:
